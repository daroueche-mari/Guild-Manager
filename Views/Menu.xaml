<Window x:Class="GuildManager.Menu"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GuildManager"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Title="Menu Principal - Guild Manager" Height="650" Width="1050" Background="#141414">

    <Window.Resources>
        <!-- Style réutilisable pour les boutons du Menu -->
        <Style x:Key="MenuButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#2A2A2A"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
            <Setter Property="Height" Value="50"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                BorderBrush="#FFFFCE00" BorderThickness="1" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#B2F10000"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#7F000000"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <!-- Image de fond / Bannières supérieures -->
        <Image x:Name="SupportTitreMenu" HorizontalAlignment="Center" Height="250" Margin="0,-60,0,0" VerticalAlignment="Top" Width="750" Source="/assets/Ui/Background/Background_Top.png"/>

        <!-- TITRE DU JEU -->
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,50,0,0">
            <TextBlock Text="GUILD MANAGER" FontSize="36" FontFamily="Castellar" FontWeight="Bold" Foreground="#FFFFCE00" HorizontalAlignment="Center"/>
            <TextBlock Text="M E N U   P R I N C I P A L" FontSize="13" Foreground="#FFB20000" HorizontalAlignment="Center" Margin="0,5,0,0" FontWeight="Bold"/>
        </StackPanel>

        <!-- CONTENEUR CENTRAL (BOUTONS RESTANTS) -->
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="320" Margin="0,50,0,0">

            <!-- BOUTON ACCÉDER AUX QUÊTES -->
            <Button Content="Accéder aux Quêtes" Style="{StaticResource MenuButtonStyle}" Click="OpenMainWindowBtn_Click"/>

            <!-- BOUTON QUITTER -->
            <Button Content="Quitter le Jeu" Style="{StaticResource MenuButtonStyle}" Click="QuitGameBtn_Click"/>

        </StackPanel>

        <!-- INFORMATIONS / PIED DE PAGE -->
        <DockPanel VerticalAlignment="Bottom" Margin="20,0,20,15">
            <TextBlock Text="Version 1.0.0" Foreground="#666666" FontSize="11" VerticalAlignment="Center"/>
            <TextBlock Text="Projet GuildManager" Foreground="#666666" FontSize="11" HorizontalAlignment="Right" VerticalAlignment="Center"/>
        </DockPanel>
    </Grid>
</Window>