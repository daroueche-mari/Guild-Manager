<Window x:Class="GuildManagerProjet.GameWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GuildManagerProjet"
        mc:Ignorable="d"
        Title="GameWindow" Height="650" Width="1050" Background="#FF292929">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- BARRE SUPÉRIEURE (Gold / Level) -->
        <Border Grid.Row="0" Background="#FF505050" BorderBrush="#CBA758" BorderThickness="1" Padding="5" Margin="0,0,0,10">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Border BorderBrush="#CBA758" BorderThickness="1" Padding="20,2" Margin="0,0,30,0">
                    <TextBlock x:Name="GoldTextBlock" Text="Gold" Foreground="#CBA758" FontSize="14"/>
                </Border>
                <Border BorderBrush="#CBA758" BorderThickness="1" Padding="20,2">
                    <TextBlock x:Name="LevelTextBlock" Text="Level" Foreground="#CBA758" FontSize="14"/>
                </Border>
            </StackPanel>
        </Border>

        <!-- TITRE PRINCIPAL + BOUTON RETOUR -->
        <Border Grid.Row="1" Background="#FF1E1E1D" BorderBrush="#CBA758" BorderThickness="1" Padding="8" Margin="0,0,0,10">
            <Grid>
                <TextBlock Text="PHASE DE RESOLUTION" Foreground="#8B0000" FontSize="22" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                <Button Content="Retour aux quetes" x:Name="BackMain" Background="#388E3C" Foreground="White" Padding="10,4" HorizontalAlignment="Right" Click="ToBackMain"/>
            </Grid>
        </Border>

        <!-- ZONE DU JOURNAL DE L'AVENTURE -->
        <Border Grid.Row="2" Background="#8A8061" BorderBrush="#CBA758" BorderThickness="1">
            <Grid Background="#FF1E1E1D">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" BorderBrush="#CBA758" BorderThickness="0,0,0,1" Padding="10,6">
                    <TextBlock Text="Journal de l'aventure" Foreground="#1565C0" FontWeight="Bold" FontSize="14"/>
                </Border>

                <Border Grid.Row="1" BorderBrush="#CBA758" BorderThickness="1" Margin="15" Padding="10">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <TextBlock x:Name="NarrationTextBlock" Text="TextBox" Foreground="#CBA758" FontSize="14"/>
                    </ScrollViewer>
                </Border>
            </Grid>
        </Border>
    </Grid>
</Window>
